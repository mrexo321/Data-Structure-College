#include <stdio.h>
int main()
{
    int number = -1;
    if (number > 0)
    {
        printf("angka %d " , number);
        printf(" Angka positif");
    }
    else
    {
         printf("angka %d" , number);
        printf(" Angka negatif");
    }
    return 0;
}
